import React, { Component } from "react";
import Axios from "axios";

class AttendForm extends Component {
  state = {
    event: null
  };
  // TODO Retrieve event ID from event page
  // TODO Retrieve user id then assign to attendee Schema
  // TODO Create redux form for user to enter friends detail and dependents
  // TODO Store data into event's attribute through the attendees Schema

  render() {
    // const { _id } = this.state.event;
    return <div></div>;
  }
}

export default AttendForm;
